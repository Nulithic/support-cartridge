import { Box, Typography, Container } from "@mui/material";

export default function Recycle() {
  return (
    <Box sx={{ padding: "100px" }}>
      <Container sx={{ background: "transparent" }}>
        <Typography variant="h4">Recycle:</Typography>
      </Container>
    </Box>
  );
}
